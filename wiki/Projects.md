# Projects Index

- deer-flow — Python/TypeScript AI project (use pnpm for frontend). Key commands: `uv sync`, `pytest`.
- potpie — Python knowledge graph. Key commands: `uv sync`, `pytest`, `ruff`.
- zeroclaw — Go assistant. Key commands: `make build`, `make build-launcher`.
- custom-mmo — Kotlin/Gradle multi-module game. Key commands: `./gradlew build`, `./gradlew :server:run`.
- router-tool — Node/Puppeteer utility. Key commands: `cd router-tool && npm install && node get-wifi.js`.
- osrs-launcher — Java single-file clients compiled with javac.

Each project typically documents its own test/run steps in its README. Use the copilot-instructions.md for single-test invocation patterns per language.
