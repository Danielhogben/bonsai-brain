# Workspace Overview

This workspace is a multi-project developer environment. Each top-level folder is usually an independent project with its own README and build tooling. Key projects include deer-flow, potpie, zeroclaw, custom-mmo, router-tool, and osrs-launcher.

Before running commands: cd into the target project directory and consult that project's README.

